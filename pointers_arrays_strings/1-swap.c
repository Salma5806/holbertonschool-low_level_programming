#include "main.h"
/**
 * swap_int - swaps thee values of two integers.
 * @a: the first pointer to an int.
 * @b: the second pointer to an int.
 */
void swap_int(int *a, int *b)
{

	tmp = *a
	*a = *b;
	*b = temp;
}
