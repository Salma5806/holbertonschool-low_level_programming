#include <stdio.h>

/**
 * prints the numbers from 1 100.
 *//
{
	int i;

{
 
	for (i = 1; i <= 100; i++)

	{
		if ((i % 3) == 0 ) 
			printf("Fizz");

		else if ((i % 5) == 0)
			printf("Buzz");

		else (((i % 3) == 0) && (i % 5) == 0))
			printf("Buzz");

	}
	printf("\n");
}

