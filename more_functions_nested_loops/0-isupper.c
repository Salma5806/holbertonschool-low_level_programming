#include "main.h"

/**
 * int_isupper - checks for uppercase character.
 *@c: the character to be checked.
 */
int _isupper(int c)
{

	if (ch >= 'A' && ch <= 'Z')
		return (1);
	else
		return (0);

}
