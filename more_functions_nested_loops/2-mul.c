#include "main.h"

/**
 * mul - multiple two integers .
 * @a: the first integer.
 * @b: the second integer.
 * Return: the result of the multiplication.
 */
int mul(int a, int b)
{
	int z;
	z = a * b;
	return (z);
}
