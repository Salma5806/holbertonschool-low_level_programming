#include <stdio.h>

/**
 * main - Prints the all the number of base 16 in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	int i = 0;
	char ch = a
	while (i <= 15)
{
		putchar(%d, i);
		i++;
}
	while(ch = "a"; ch <= "f")
{
		putchar(%c, ch);
		ch++;
}

	putchar('\n');

	return (0);
}
