#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a  ;
	printf("enter the number : ");
	scanf("%d" , &a) ;
	if (a>0)
	printf("is positive")
	else
	printf("is negative")

return (0);
}

