#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;
	printf("enter the number : ");
	scanf("%d" , &a) ;
	if (a>0)
	printf("%d is positive\n",a);
	else if (n == 0)
	printf("%d is zero\n",a);
	else
	printf("%d is negative\n",a);

return (0);
}

